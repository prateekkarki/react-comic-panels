import styled from '@emotion/styled';

export const PanelImageContainer = styled.div`
  pointer-events: none;
  user-select: none;
  max-width: 100%;
  max-height: 100%;

  & img {
    display: block;
    max-width: 100%;
    max-height: 100%;
  }
`; 